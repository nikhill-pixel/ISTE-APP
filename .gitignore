# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history/             # Ensure this is a directory
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/
.idea/workspace.xml    # Explicitly ignore common user-specific files
.idea/tasks.xml
.idea/dictionaries
.idea/shelf/
.idea/dataSources/     # For database connections

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default. Uncomment if you want to ignore VS Code settings.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# IDE/OS specific ignore
# macOS
.fseventsd/
.Spotlight-V100/
.Trashes/
.DocumentRevisions-V100/
.TemporaryItems/

# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini

# Android specific
android/.gradle/
android/app/build/
android/app/intermediates/
android/app/outputs/
android/app/*.apk
android/app/*.aab
android/captures/
android/gradlew       # Generally commit gradlew for cross-platform builds
android/gradlew.bat   # Generally commit gradlew.bat for cross-platform builds
android/local.properties # Contains local SDK paths, specific to each dev env
android/.idea/
# For Android keystore files (uncomment if you use local JKS/Keystore for signing)
#*.jks
#*.keystore
#google-services.json # This file contains Firebase configuration, generally safe but some prefer to ignore for added caution if not using a .env system.
#                     # If you ignore this, each developer needs their own or you need to manage sharing it.

# iOS specific
ios/Pods/                 # Generated by CocoaPods
ios/.symlinks/           # Generated by CocoaPods
ios/Flutter/App.framework/
ios/Flutter/Flutter.framework/
ios/Flutter/Generated.xcconfig
ios/Flutter/ephemeral/
ios/Flutter/app.flx
ios/Flutter/app.zip
ios/Flutter/flutter_assets/
ios/ServiceDefinitions.json
ios/Runner/*.xcworkspace/
ios/Runner/project.xcworkspace/
ios/Runner/xcuserdata/ # Xcode user-specific settings

# Sensitive files / API Keys / Environment Variables
# Crucially, add your Firebase options file here if you want to keep it out of public Git
lib/firebase_options.dart

# For environment variables
.env
# If you use envied, also ignore the generated env file if it's not meant to be committed
# lib/env/env.g.dart # Example if envied generates a file here

# Generated code files (from build_runner, freezed, json_serializable, etc.)
# Generally, you should *not* ignore these if your project uses code generation.
# Committing them ensures all developers have the same generated code without running build_runner.
# If you *do* want to ignore them, uncomment the following line.
# *.g.dart
# *.freezed.dart

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release